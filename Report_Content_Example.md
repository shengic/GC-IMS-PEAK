# GC-IMS Peak Identification Report

**Version: 3.3 — by Albert Sheng**

**這是「Generate Report」按鈕輸出內容的範例——示範內容結構，不是實際格式決定**
（CSV／JSON／docx／PDF 等實際匯出格式仍待後續討論，這份只是要讓您看「裡面該有什麼」）

---

## 1. 樣品與儀器資訊（可追溯性 — provenance）

| 欄位 | 值 |
|---|---|
| 樣品名稱 | STD |
| 來源檔案 | 260625_141215_STD.mea |
| 機型 | FlavourSpec®（序號 5H4-00123） |
| 管柱 | FS-SE-54-CB-1（30.00 m，內徑 0.53 mm，膜厚 0.50 µm，非極性） |
| 漂移氣體 | nitrogen |
| 量測時間 | 2026-06-25T14:12:15 |
| 儀器狀態 | doubtful（3 筆待處理診斷，見下方警示區） |
| 報告產出時間 | 2026-07-16 14:32 |

> **儀器狀態警示**：此次量測被儀器自身標記為 `doubtful`（`FlowGClow`／`SeptumDurability`／`NoValidSnapshot`），結果解讀時建議留意。

---

## 2. 找峰與篩選參數（可重現性 — reproducibility）

| 項目 | 值 |
|---|---|
| 找峰演算法參數 | `sigma=1.0`, `floor_pct=85`, `min_distance=3` |
| 已啟用規則 | `R001`（min_prominence=50）、`R004`（exclude_rip_band, half_width=0.02）、`R005`（min_relative_steepness=0.3） |
| 候選數量漏斗 | 276,713（原始局部極大）→ 47（規則篩選後）→ 45（使用者勾選保留） |

---

## 3. 校準狀態（可信度聲明 — 誠實揭露未解決事項）

| 校準項目 | 狀態 |
|---|---|
| RI 校準 | 六點烷烴校準曲線，已用 ethanol-M 交叉驗證（誤差 0.013） |
| K0 校準 | ⚠️ **`raw_parameters` 模式，未經真實標準品驗證**（詳見 workflow 文件第二階段「明確的未解決狀態聲明」）——本報告內 K0 相關比對結果應視為暫定值 |

---

## 4. 已識別峰清單

| # | RT [s] | DT [ms] | Intensity | 確認化合物 | CAS | GC×IMS 候選數 | 來源資料庫 |
|---|---|---|---|---|---|---|---|
| 1 | 142.20 | 8.34 | 1444 | **Hexanal**（已確認） | 66-25-1 | 2 | RTX-5Sil_MS.ril, GAS_BASE_3H_IMS.iml |
| 2 | 153.00 | 14.00 | 892 | *(未確認)* | — | 0 | — |
| 3 | 165.60 | 21.33 | 650 | **2-Heptanone**（已確認） | 110-43-0 | 1 | AVERAGE_LOW_POLAR.ril, GAS_BASE_3H_IMS.iml |
| … | … | … | … | … | … | … | … |

**未識別峰**（三個維度皆無候選）：12 個，peak_id 清單另附。

---

## 這份範例想說明的幾個設計重點

1. **第 1、2 區塊是「可追溯性」與「可重現性」**——不是分析結果本身，但沒有這些，這份報告單獨拿出來就無法確認是哪次量測、用了什麼參數跑出來的。
2. **第 3 區塊刻意把未解決的 K0 校準問題寫進報告本身**，不是只藏在工作流程文件裡——如果這份報告將來要給別人看，他們沒讀過您的 workflow 文件，**報告本身就要老實告知「這部分數字還不完全可信」**，不能假裝校準已經做完。
3. **「確認化合物」欄只顯示使用者按過 Confirm 的結果**，沒按 Confirm 的峰維持「未確認」——呼應「自動比對、點擊是雙重確認」這個原則，報告只記錄人工確認過的結論，不是演算法的第一名候選。
4. **`Generate Report` 應該只匯出 `On`（`active=true`）的峰**——使用者已經取消勾選的峰不該出現在最終報告裡，這點文件目前沒明講，一併定案。

## 待您確認的問題

- 第 4 區塊「已識別峰清單」要不要也把 `GC only`／`IMS only` 的候選數量欄位放進來（跟 UI 表格一致），還是報告只保留最終確認結果，簡化掉中間過程？
- 要不要附上 `peak_with_number.py` 產生的標號圖當附件？
- 這份報告的**受眾**是誰（自己存檔備查、還是要給別人看）會影響格式選擇——存檔備查用 CSV/JSON 夠用，要給人看可能需要 docx/PDF 排版。
