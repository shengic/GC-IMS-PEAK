# 第二支應用 — 進度與交接（status2）

**Version: 1.0 — by Albert Sheng**
**最後更新**：2026-08-24
**用途**：讓新的 session 快速接手**第二支應用**（區域強度矩陣）。第一支應用的進度在
`status.md`，兩份互不覆蓋。

> **先讀哪一份**：想知道「這支怎麼用」讀 `README2.md`；想知道「為什麼這樣設計、
> `.gasprj` 裡有什麼」讀 `Area_Matrix2.md`；想知道「做到哪、還缺什麼」讀本檔。

---

## TL;DR

- **可用，端到端跑得動。** 實測 `GAS/Coffee-bean`：**57 區域 × 15 檔案**，格子 100%
  有值；共識模式另測得 **79 區域 × 18 檔案**。
- **與第一支應用完全隔離**：不修改任何既有 `.py` / `.md`，產物一律帶 `2`。
  `git status` 只會看到新增檔案。
- **44 項測試**：`test2/test_areas2.py`（31，邏輯）+ `test2/test_main2.py`（13，介面）。全套 238 項通過。
  介面測試是本輪補的——先前第二支應用的 UI **一項測試都沒有**，而這一輪修掉的
  問題全在 UI 層（資料夾陷阱、執行緒無聲死掉、`?` 標題、跑完亂塗綠），當時是用
  臨時腳本抓出來、抓完就丟。現在固定成回歸測試。
- **速度**：Fast 模式下 15 檔 **12 秒**；完整共識模式首次約 18 分鐘（之後走快取）。
- **尚未做**：跨檔保留時間對齊、嚴謹的峰體積積分、統計檢定（組間差異）。

---

## 這支應用在做什麼（一段話）

拆解 `GAS/Coffee-bean/rawbean-20260625.gasprj` 後發現 VOCal 的模型與第一支應用不同：
一個 `.gasprj` 是**跨 15 個 `.mea` 的專案**，存著 **57 個 `MeasAreas`**——在
(漂移, 保留) 平面上**畫一次**、然後在**每一個檔案**上都量一次的固定方框。
**VOCal 不逐檔重新找峰**，因為各檔獨立找出的峰對不齊就無法比較分組。本應用照這個
模型做，同時保留既有的自動找峰、drift/RI 座標正規化與 6 點 STD 校正。

兩個座標本專案早就有了：`RipRel` ＝ `drift_relative`，`SpecNum` ＝ `rt_index`
（實測：方框中心 × `(averages+1) × trigger_repetition` 重現 VOCal 自報的 `Rt[sec]`，
22 個化合物比值 0.992–1.005）。

---

## 現況

| 項目 | 狀態 | 備註 |
|---|---|---|
| `.gasprj` 讀取（Entries / MeasAreas / Class） | ✅ | 唯讀；座標型別非 `RipRel`+`SpecNum` 一律跳過不臆測 |
| 逐檔找峰（沿用既有管線） | ✅ | 函式呼叫，不跑 `peaks.py` CLI，不寫第一支應用的產物 |
| 資料夾校正（RI + K0） | ✅ | `resolve_calibrations_cached(use_sidecar=False)`，四層 RI 全支援 |
| 共識區域（跨檔群聚） | ✅ | 唯一的新演算法；容差取自 VOCal 方框中位數 |
| 區域量測（volume/max/mean） | ✅ | 量不到回 `None` 不是 `0` |
| 區域命名 | ✅ | `match.match_all()`；`.gasprj` 匯入時**保留 VOCal 的名字**；選庫極性跟著實際 RI 尺標（見 `status.md` open decision 9） |
| `Class` 標籤一致性檢查 | ✅ | `check_class_labels()`，只報不改 |
| CSV / JSON 輸出 | ✅ | UTF-8 BOM，Excel 直接開 |
| Tk 介面 | ✅ | 摘要／逐檔兩種視圖、即時進度、檔案上色 |
| Fast 模式（跳過找峰） | ✅ | 只能配 `.gasprj` 區域；共識模式會直接報錯 |
| 跨檔保留時間對齊 | ❌ | 見「下一步」 |
| 組間統計檢定 | ❌ | 見「下一步」 |

---

## 1.0 開發期間修掉的問題（都是實際使用回報）

這幾條記下來，是因為**症狀與原因差很遠**，重看程式碼不容易想到：

| 症狀 | 真正的原因 |
|---|---|
| 按 Run batch 沒反應 | 檔案選擇器預設開在 `GAS/`，而它底下只有子資料夾、沒有 `.mea` → 0 個樣品 → 按鈕靜靜變灰 |
| 矩陣永遠空白、**也沒有錯誤** | `build_matrix` 原本 `raise SystemExit`（`BaseException`），背景執行緒的 `except Exception` 接不到 → 執行緒無聲死掉、佇列永遠空著、UI 一直等 |
| 看不出程式有沒有在跑 | 訊息面板只接得到 `areas2` 自己的十來行；最花時間的 `peaks.py` 找峰用普通 `print()`，只進主控台 |
| 欄位標題全是 `?` | (a) `Class` 只在 `from_gasprj` 時才讀，共識模式整組遺失；(b) 標題用了換行，但 ttk.Treeview 的 heading 只畫一行 |
| 摘要視圖分成 13 組、每組 n=1 | `Class` 存的是**每個重複樣品**的代號（`A 1-1`/`A 1-2`），不是組別；要取第一個 token 才還原成 A/B/C/D/E |
| 批次很久 | 找峰佔 **75%**（單檔 union-find 55.4 s），而方框來自 `.gasprj` 時它只用來填 `n_det` |

> **一個與程式無關但值得記的坑**：暫存資料夾放了名為 `grp.py` 的臨時腳本，而
> `pathlib` 會 `import grp`——scipy 的匯入鏈因此在深處炸出 `AssertionError`，看起來
> 像 scipy 壞了。**不要用標準函式庫的模組名當臨時檔名**（同一輪還踩過 `struct.py`）。

---

## 隔離規則（改動前必讀）

三條，每一條都有對應的測試把關：

1. **扣過基線的資料絕不寫回共用的 `.npz`**——否則 `main.py` 會把它當原始值載入而
   毫無跡象。`test_baseline_never_written_back_to_npz`。
2. **不寫任何第一支應用的逐檔產物**（`_peaks.json` / `_maxima.npz` / `_bg.png` …）。
   以函式呼叫 `peaks.detect_peaks()`，不跑它的 CLI。
3. **不寫任何東西進 `GAS/`**——`use_sidecar=False`，`.mea` / `.gasprj` 唯讀。

---

## 實測數字（留著當回歸基準）

```
GAS/Coffee-bean（15 檔，.gasprj 區域，Fast，不扣基線）
  57 區域 × 15 檔案，格子 100% 有值，12 秒
  ri_mode=batch_own_std   k0_mode=standard_based   命名 30/57

GAS/Coffee-bean（18 檔，共識區域）
  79 區域 × 18 檔案

單檔耗時：union-find 55.4 s（75%）· 基線 16.8 s（23%）· 其餘 1.8 s
分組：A/B/C/D/E 各 3 個重複
```

**一項意外的交叉驗證**：我們用 6 點酮校正算出的區域 RI，與 VOCal 自己曲線給的值
高度一致——`1- butanol` 1144.1 vs 1144.9、`2-Butanone` 903.2 vs 909.6、
`ethyl acetate-M` 894.6 vs 900.2。兩條獨立的校正路徑差在 ~6 RI 以內。

---

## 下一步（依價值排序）

1. **組間統計** —— 矩陣已經有了，但目前只到「每組平均」。要回答「A 與 B 有沒有顯著
   差異」還需要組內變異、檢定與多重比較校正。這是這支應用真正的目的地。
2. **跨檔保留時間對齊** —— 現在靠方框夠寬吸收漂移。若哪一批漂移超出方框就會漏量，
   而且**不會有任何徵兆**。`gc-ims-tools` 有 `align_ret_time` 可參考（僅參考演算法，
   不引入套件，同 `baseline.py` 的作法）。
3. **共識區域與 VOCal 的 57 個方框對照** —— `--from-gasprj` 已經讓兩者可以逐格比，
   但還沒有系統性跑過。差異大的地方就是群聚容差要調的地方。
4. **峰體積積分** —— 目前 `volume` 是方框內高於 floor 的簡單加總。要真的定量需要
   峰形擬合或邊界判定。
5. **`Class` 標籤與檔名不一致** —— ✅ **已會自動偵測並回報**（2026-08-24）：
   `check_class_labels()` 逐檔比對 `Class` 與檔名的樣品代號，跑批次時寫進 Progress log
   與 `class_warnings`。實測 Coffee-bean **3/15** 不一致（`E_1_2` 標成 `E 1-1`、
   `C_1_2` 標成 `C 1-1`、`C_1_3` 標成 `C 1-2`）。
   **只報不改**——哪一邊才對是原始資料的問題，程式沒有依據判斷，靜默改掉更糟。
   **做組間統計前仍必須先確認哪一邊是對的**，這一項因此還沒真正關掉。

---

## 未做、且刻意不做的事

- **不修改任何既有 `.py` / `.md`**（使用者指定）。因此 `status.md` / `README.md` /
  `CLAUDE.md` 都不會提到第二支應用——這幾份 `2` 檔案是自足的。日後若要讓新 session
  自動找得到，在 `status.md` 加一行指標即可。
- **不動 RI 尺標的爭議**：管柱極性的問題（見 `ketone_RI_provenance.md`）原樣傳遞到
  本應用的 provenance，不在這裡另做判斷。
