# 甲基酮 RI 數值來源記錄 —— 暫定採用，待更好數字時回頭替換

**狀態：暫定採用（借用），非本批次自建，非最終定案**
**用途**：這份文件記錄「為什麼現在用這六個數字」、「這六個數字有多可信」、「以後有更好的數字時要怎麼替換」。任何人（包含未來的你、CLI）在改動 `reference_series.py` 的 `methyl_ketone.ri_values` 之前，先讀這份。

---

## 現在採用的數字

來源：`Auto_Project_Backup.gasprj`（**海洋大學水產養殖系「鱸魚肉風味影響」專案**，非本批次 `260625_*_STD.mea` 自建）的 `RI_Normalization` 區塊，六點原始資料：

```python
METHYL_KETONE_RI = {
    "2-butanone":  589.4,   # C4
    "2-pentanone": 688.6,   # C5
    "2-hexanone":  784.2,   # C6
    "2-heptanone": 892.2,   # C7
    "2-octanone":  996.5,   # C8
    "2-nonanone":  1095.6,  # C9
}
```

對應的 `ColNormX`（`log10(Rt秒)`，反推可得原始 Rt）：

| 化合物 | RI | log10(Rt) | Rt (s) |
|---|---|---|---|
| 2-butanone | 589.4 | 1.8188 | 65.9 |
| 2-pentanone | 688.6 | 1.9200 | 83.2 |
| 2-hexanone | 784.2 | 2.0932 | 123.9 |
| 2-heptanone | 892.2 | 2.3271 | 212.4 |
| 2-octanone | 996.5 | 2.5257 | 335.5 |
| 2-nonanone | 1095.6 | 2.7255 | 531.5 |

**注意**：這是**鱸魚專案自己的 Rt**（不同管柱狀態/批次），不是你的 `260625_141215_STD.mea` 那六個峰的 Rt。套用到你自己的 STD 時，只借用 RI 數值（Y 軸），Rt 用你自己實測的那六個（282.0/334.3/400.3/521.8/697.0/949.0秒），兩邊的 X 軸不能混用。

---

## 為什麼選這組，不選其他來源

比對過三個獨立來源：

| 來源 | 2-butanone RI | 2-heptanone RI | 可信度 |
|---|---|---|---|
| **`Auto_Project_Backup.gasprj`（採用）** | 589.4 | 892.2 | 六點原始資料，非密集重取樣，無異常值 |
| 外部文獻估計（非極性管柱，未逐篇查證） | 590–610 | 885–900 | 與上者高度吻合（差距<11），但來源本身未經查證，僅供交叉比對 |
| `ML-TEST20260629.gasprj`（**不採用**） | 909.6 | 1183.7 | **校正曲線本身有負值外插**（`ColNormX=1.8638→ColNormY=-930.4`），判定曲線品質不合格，此曲線算出的化合物 RI 一併视为不可信 |

前兩者互相印證（差距全部在 11 個 RI 單位內），第三者被判定不可信的具體理由是**曲線本身出現物理上不可能的負 RI 值**，屬於「Use Fit」機制外插失控的實例（見 `GC-IMS_Identify_Workflow.md` draft.16 第 4 點的機制說明）。

---

## 這組數字的信心等級：借用，非本批次自建，非化合物身分確認

三層限制要清楚知道：

1. **借用另一個專案的校正表，不是你自己這批 STD.mea 校出來的**——即使管柱型號一致，實際的儀器狀態、當天溫濕度、管柱老化程度都不是同一次量測，套用到 `260625_*_STD.mea` 這批資料上，本質上跟 `GC-IMS_Identify_Workflow.md` 已定案的 `resolve_ri_calibration()` 三層邏輯裡的**情境二（跨批次借用）**是同一件事，必須帶對應的信心標記。
2. **化合物身分本身仍是「使用者確認為酮，C4–C9」這個推斷，不是逐一比對過質譜或標準品證書確認的**。
3. **外部估計數字的原始來源未逐篇查證**——只做了交叉比對，沒有逐一核對 NIST WebBook 或原始文獻，比對吻合只能提高信心，不能取代查證。

---

## 實作標記（`reference_series.py`）

```python
REFERENCE_SERIES["methyl_ketone"] = {
    "assumed": True,
    "ri_values": [589.4, 688.6, 784.2, 892.2, 996.5, 1095.6],
    "provenance": "borrowed_from_Auto_Project_Backup.gasprj（鱸魚專案），非本批次自建，"
                  "與外部非極性管柱估計值交叉比對誤差<11個RI單位（來源未逐篇查證），"
                  "ML-TEST20260629.gasprj 的數字（909.6/1183.7）因校正曲線本身有負值外插，判定不可信、不採用",
    "confidence": "borrowed_cross_referenced",  # 不是 "verified"，也不是本批次 "self_calibrated"
}
```

`assumed_unverified=True` 這個標記（見 `RT_to_RI_normalization_math.md`）在這組數字上依然要維持——借用+交叉比對可以提高信心，但不等於驗證完成。

---

## 什麼時候可以升級成「已驗證」，回來改這份文件

以下任何一項到位，就回來更新本文件跟 `reference_series.py`：

- [ ] 拿到 STD 標準品證書或供應商資料，直接寫明這六個化合物與各自的 RI/純度
- [ ] 用你自己的 `260625_*_STD.mea` 實際跑出一支**乾淨、無負值外插**的六點校正表（不透過 `Use Fit`，或確認 `Use Fit` 的擬合範圍完全落在有效資料區間內）
- [ ] 查到有逐篇查證來源的非極性管柱（SE-54 或極相近相位）文獻 RI 值，且與現在這組數字吻合或有明確差異需要修正
- [ ] 操作者回憶或找到當初配製 STD 標準品時的紀錄

升級後，`confidence` 欄位改成 `"self_calibrated"` 或 `"literature_verified"`，`assumed` 改成 `False`，`RT_to_RI_normalization_math.md` 裡的示範 RI 值（400/500/600/700/800/900）也該同步換成這組真實數字，讓文件裡不再有「示範用途」跟「實際校準值」混淆的風險。
