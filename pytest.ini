# 兩個測試根目錄：test/ 是第一支應用，test2/ 是第二支應用。
#
# `testpaths` 是**防呆**，不是排版偏好：文件上長年寫的指令是 `pytest test/ -q`，
# 分家之後照那個指令跑會靜靜漏掉第二支應用的 44 項測試——而「漏掉」不會有任何徵兆，
# 正是本專案一再防範的那類問題。有了這一行，光打 `pytest` 就會兩邊都收。
[pytest]
testpaths = test test2
